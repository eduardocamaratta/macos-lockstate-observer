// swift-tools-version: 5.7

import PackageDescription

let package = Package(
    name: "LockStateObserver",
    targets: [
        .executableTarget(
            name: "lock-state-observer"
        )
    ]
)
