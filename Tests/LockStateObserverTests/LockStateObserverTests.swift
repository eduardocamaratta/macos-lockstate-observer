import XCTest
@testable import LockStateObserver

final class LockStateObserverTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(LockStateObserver().text, "Hello, World!")
    }
}
